spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/vulnshop?useSSL=false&serverTimezone=UTC&characterEncoding=utf8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

#  h2:
#    console:
#      enabled: true
#      path: /h2-console
server:
  port: 8080
  servlet:
    context-path: / 